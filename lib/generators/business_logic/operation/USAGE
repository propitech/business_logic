Description:
    Generates a new operation and its spec file.

Example:
    bin/rails generate business_logic:operation CreateUser

    This will create:
        app/operations/create_user.rb
        spec/operations.create_user_spec.rb
