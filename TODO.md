# TODO

- Decide whether `mise.toml` (pins ruby to 3.4.5) should be the
  source of truth for the project's ruby version, replacing
  `.ruby-version`, or whether both should live side by side. If
  keeping both, document the precedence in README §"Development".
