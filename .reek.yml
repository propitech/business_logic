---
# IrresponsibleModule disabled here because rubocop's
# Style/Documentation enforces the same rule — keeping both
# produces duplicate findings for every spec fixture and helper.
detectors:
  IrresponsibleModule:
    enabled: false
